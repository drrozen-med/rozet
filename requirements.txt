# This project is managed with uv. Do not edit this file.
# Install dependencies with:
#   uv sync --extra dev
# or regenerate lockfile with:
#   uv pip compile pyproject.toml --extra dev --output-file uv.lock
