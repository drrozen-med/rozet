[pytest]
markers =
    integration: marks tests as integration tests (requires real API calls)
    unit: marks tests as unit tests (fast, no API calls)
    e2e: marks tests as end-to-end tests (full workflows)


